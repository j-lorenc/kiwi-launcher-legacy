import { hot } from 'react-hot-loader/root';
import * as React from 'react';

export const Application: () => JSX.Element = () => {
  return <div>New Launcher</div>;
};

export default hot(Application);
